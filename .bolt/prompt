For all designs I ask you to make, have them be beautiful, not cookie cutter. Make webpages that are fully featured and worthy for production.

By default, this template supports JSX syntax with Bootstrap 5 classes, React hooks, and Lucide React for icons. It's fine to use `react-bootstrap` for components and Bootstrap utilities for layout.

Use icons from lucide-react for logos.
